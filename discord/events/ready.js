module.exports = async (client) => {
	console.log(`Discord bot has started, with ${client.discord.users.size} users, in ${client.discord.channels.size} channels of ${client.discord.guilds.size} guilds.`); 
	//client.user.setActivity("with ice");
	
	
};